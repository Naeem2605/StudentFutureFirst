StudentFutureFirst 

How to run locally:

Tools Required:
Python 3.12.8
Django 5.0.6

Install requirements.txt using terminal:
pip install -r requirements

create .env file using the template given in .env.example file and generate any random secret key desired

Run development server:
python manage.py runserver

Head to the link given in the terminal
